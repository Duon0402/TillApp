<UserControl x:Class="TillApp.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Cột trái: danh sách sản phẩm -->
        <ScrollViewer Grid.Column="0" Margin="0,0,12,0">
            <ItemsControl ItemsSource="{Binding Products}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="140" Height="100" Margin="6" Padding="10"
                                Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Price, StringFormat={}{0:N0} đ}" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Cột phải: giỏ hàng -->
        <Border Grid.Column="1" Background="#F3F4F6" CornerRadius="6" Padding="12">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Giỏ hàng" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

                <StackPanel DockPanel.Dock="Bottom">
                    <Separator Margin="0,8"/>
                    <TextBlock Text="{Binding GrandTotal, StringFormat={}Tổng cộng: {0:N0} đ}" FontSize="16" FontWeight="Bold"/>
                    <Button Content="Thanh toán" Margin="0,12,0,0" Padding="0,8" IsEnabled="False"/>
                </StackPanel>

                <ListView ItemsSource="{Binding CartItems}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="-" Width="24" Command="{Binding DecreaseCommand}"/>
                                <TextBlock Grid.Column="2" Text="{Binding Qty}" Width="24" TextAlignment="Center" VerticalAlignment="Center"/>
                                <Button Grid.Column="3" Content="+" Width="24" Command="{Binding IncreaseCommand}"/>
                                <TextBlock Grid.Column="4" Text="{Binding LineTotal, StringFormat={}{0:N0} đ}" Width="80" TextAlignment="Right" VerticalAlignment="Center"/>
                                <Button Grid.Column="5" Content="Xóa" Margin="8,0,0,0"
                                        Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>