<UserControl x:Class="TillApp.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Cột trái: danh sách sản phẩm -->
        <ScrollViewer Grid.Column="0" Margin="0,0,12,0">
            <ItemsControl ItemsSource="{Binding Products}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="140" Height="100" Margin="6" Padding="10"
                                Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                                CornerRadius="6">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Price, StringFormat={}{0:N0} đ}" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Cột phải: giỏ hàng -->
        <Border Grid.Column="1" Background="#F3F4F6" CornerRadius="6" Padding="12">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Giỏ hàng" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

                <StackPanel DockPanel.Dock="Bottom">
                    <Separator Margin="0,8"/>
                    <TextBlock Text="{Binding GrandTotal, StringFormat={}Tổng cộng: {0:N0} đ}" FontSize="16" FontWeight="Bold"/>
                    <Button Content="Thanh toán" Margin="0,12,0,0" Padding="0,8" IsEnabled="False"/>
                </StackPanel>

                <ListView/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>