<UserControl x:Class="TillApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="280">
        <TextBlock Text="Đăng nhập" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Text="Tài khoản"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12"/>

        <TextBlock Text="Mật khẩu"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,4,0,12"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Margin="0,8,0,8"/>

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red" TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>
