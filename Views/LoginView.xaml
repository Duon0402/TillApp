<UserControl x:Class="TillApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{StaticResource CardStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="320">
        <StackPanel>
            <TextBlock Text="Đăng nhập" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Tài khoản" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>

            <TextBlock Text="Mật khẩu" Style="{StaticResource FieldLabelStyle}"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,18"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <Button Content="Đăng nhập" Command="{Binding LoginCommand}"/>

            <TextBlock Text="{Binding ErrorMessage}" Margin="0,12,0,0"
                       Foreground="#DC2626" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>
